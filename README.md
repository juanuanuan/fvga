# FVGA
This repo was made with the purpose of analysing how Hoare Logic can be applied into graphs Algorithms 

-Formal Verification on Graph Algorithms with Hoare Logic

## Algorithms

- Warshall — 
- DFS —
- Dijkstra —

## Como correr

```
dafny run (...)MainProof.dfy --solver-path (i)
(...) -> the algorithm in question (Ex. warshallMainProof, dfsMainProof, etc.)
(i) -> z3 saved path
```
